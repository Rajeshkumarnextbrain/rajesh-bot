# --- API KEYS ---
# Replace with your actual keys
OPENAI_API_KEY=sk-your-key
GOOGLE_API_KEY="AIzaSyBhNV_nXjtdx77N4h6HZPFh9V7_ruMcL2Q"

# --- MCP CONFIGURATION ---
# In Docker, we use the service name 'mcp-server' instead of 'localhost'
MCP_TRANSPORT="sse"
MCP_PORT="8000"
ATTENDANCE_MCP_URL="http://mcp-server:8000/mcp"

# --- API SERVER CONFIGURATION ---
API_PORT="9000"
# This is the base URL for the backend API from the Frontend's perspective (External)
VITE_API_HOST="http://localhost:9000"

# --- EXTERNAL SERVICES ---
AUTH_BASE_URL="http://10.254.10.250:5000"
VISIONFACTS_EMAIL="admin@gmail.com"
VISIONFACTS_PASSWORD="Abc@12345"

# --- MODEL PROVIDER ---
PRIMARY_MODEL="gemini-3-flash-preview"
SECONDARY_MODEL="gpt-4o"
TEMPERATURE=0.5

# --- REDIS CONFIGURATION ---
# In Docker, we use the service name 'redis' instead of 'localhost'
REDIS_HOST="redis"
REDIS_PORT="6379"
REDIS_DB="0"
REDIS_PASSWORD=""
